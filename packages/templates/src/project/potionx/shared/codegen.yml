schema: http://localhost:4000/graphql/v1
generates:
  ./src/types.d.ts:
    plugins:
      - typescript
  ./src/introspection.json:
    plugins:
      - introspection
    config:
      minify: true